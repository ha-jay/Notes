자바스크립트는 흔히 '인터프리터 언어'라고 알려져 있지만, 현대의 엔진(V8 등)은 성능 최적화를 위해 **JIT(Just-In-Time) 컴파일** 방식을 사용합니다. 

엔진 내부에서 코드가 어떻게 처리되는지 그 '상세한 여정'을 단계별로 살펴봅시다.

---

## 1. 소스코드 분석: 파싱(Parsing) 단계

엔진이 코드를 읽기 시작하면 가장 먼저 하는 일은 텍스트인 코드를 의미 있는 단위로 쪼개는 것입니다.

- **토크나이징(Tokenizing):** 코드를 `let`, `a`, `=`, `10` 등 의미 있는 최소 단위인 '토큰'으로 분해합니다.
    
- **파싱(Parsing):** 분해된 토큰을 바탕으로 언어의 문법 구조를 반영한 **AST(Abstract Syntax Tree, 추상 구문 트리)**를 생성합니다. 이 단계에서 문법 오류(Syntax Error)가 발견됩니다.
    

---

## 2. 실행 컨텍스트 생성 (생성 단계)

AST가 완성되면 엔진은 **실행 컨텍스트(Execution Context)**를 생성합니다. 이때가 바로 질문하신 **'컴파일 단계'와 호이스팅**이 일어나는 시점입니다.

1. **메모리 공간 확보:** 선언된 변수와 함수를 위한 메모리 공간을 미리 예약합니다.
    
2. **호이스팅(Hoisting):** * **함수 선언문:** 함수 전체가 메모리에 등록됩니다.
    
    - **변수(var):** `undefined`로 초기화됩니다.
        
    - **변수(let, const):** 선언만 되고 초기화는 되지 않아 **TDZ(일시적 사각지대)**가 형성됩니다.
        
3. **스코프 체인 및 this 결정:** 외부 환경 참조(`Outer`)를 설정하여 스코프 체인을 형성하고 `this`가 가리킬 객체를 바인딩합니다.
    

---

## 3. 실행 단계 (Execution Phase) 및 인터프리팅

컨텍스트가 준비되면 엔진의 **인터프리터(Ignition)**가 AST를 바탕으로 **바이트코드(Bytecode)**를 생성하고 한 줄씩 실행합니다. 이때 실제 변수에 값이 할당되고 함수가 호출됩니다.

- **콜 스택(Call Stack):** 함수가 호출될 때마다 해당 함수의 실행 컨텍스트가 스택에 쌓이고(Push), 실행이 끝나면 제거(Pop)됩니다.
    

---

## 4. 최적화: JIT 컴파일러 (TurboFan)

인터프리터가 코드를 실행하는 동안 **프로파일러(Profiler)**가 코드를 감시합니다.

- **Hot Code 발견:** 자주 반복 실행되는 코드(예: 루프 내의 함수)를 감지합니다.
    
- **컴파일(Optimization):** 감지된 'Hot Code'는 **JIT 컴파일러**로 보내져 기계어(Native Code)로 직접 컴파일됩니다. 인터프리터보다 훨씬 빠르게 동작합니다.
    
- **역최적화(Deoptimization):** 만약 최적화된 코드의 타입이 갑자기 변하는 등(Dynamic Type의 특징) 가정이 틀려지면 다시 인터프리터 모드로 돌아갑니다.
    

---

## 💡 요약: 엔진의 전체 흐름

|**단계**|**수행 주체**|**주요 작업**|
|---|---|---|
|**Parsing**|Parser|코드를 토큰으로 쪼개고 **AST** 생성|
|**Creation**|Engine|**실행 컨텍스트** 생성, 호이스팅, 스코프 설정|
|**Execution**|Interpreter|바이트코드 생성 및 실행, 콜 스택 관리|
|**Optimization**|JIT Compiler|자주 쓰이는 코드를 기계어로 컴파일(최적화)|

---

### 🧐 여기서 중요한 포인트!

질문하신 "컴파일 단계"는 엄밀히 말하면 **전체 코드를 기계어로 바꾸는 단계**라기보다, **실행 환경을 구축하고 메모리를 미리 할당하는 준비 과정**과 **자주 쓰는 코드를 기계어로 최적화하는 과정**이 합쳐진 개념으로 이해하시는 것이 가장 정확합니다.

자바스크립트 엔진의 이 복잡한 과정 덕분에 우리는 동적 타입 언어임에도 불구하고 매우 빠른 실행 속도를 경험할 수 있는 것이죠.
