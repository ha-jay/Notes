논리 연산자를 활용한 **옵셔널 로직(Optional Logic)**은 코드를 간결하게 만들고, 예기치 못한 에러(Runtime Error)를 방지하는 핵심 기술입니다.
특히 객체의 깊은 곳에 접근하거나, 값이 없을 때 기본값을 설정하는 패턴을 중심으로 정리해 드립니다.

## 단락 평가 (Short-circuit Evaluation)

논리 연산자(`&&`, `||`)는 왼쪽에서 오른쪽으로 평가하며, 결과가 확정되면 나머지 부분은 아예 실행하지 않는 특징이 있습니다.

### ① AND 연산자 (`&&`) 활용: "존재하면 실행하라"

특정 변수가 `null`이나 `undefined`가 아닐 때만 다음 단계로 넘어가고 싶을 때 사용합니다.

```js
// 객체가 존재할 때만 프로퍼티에 접근
const user = { name: "Jay" };
const userName = user && user.name; // user가 참이므로 userName은 "Jay"

// 함수가 존재할 때만 호출
const callback = () => console.log("Done!");
callback && callback(); // callback이 존재하므로 함수 실행
```

### ② OR 연산자 (`||`) 활용: "값이 없으면 기본값을 써라"

좌항이 **Falsy**(false, 0, "", null, undefined, NaN)이면 우항을 반환합니다.
```js
function greet(name) {
  const userName = name || "Guest"; // name이 없으면 "Guest" 할당
  console.log(`Welcome, ${userName}!`);
}

greet(""); // Welcome, Guest! (빈 문자열을 거짓으로 취급함)
```

## 옵셔널 체이닝 (Optional Chaining: `?.`)

ES2020에서 도입된 이 연산자는 깊게 중첩된 객체 구조를 탐색할 때 **"참조가 유효한지"** 체크하며, 유효하지 않으면 에러 대신 `undefined`를 반환합니다.

```js
const user = {
  profile: {
    // address 속성이 없는 상태
  }
};

// 기존 방식: 에러 방지를 위해 수동 체크
// const city = user && user.profile && user.profile.address && user.profile.address.city;

// 옵셔널 체이닝 방식: 훨씬 깔끔함
const city = user?.profile?.address?.city; 
console.log(city); // undefined (에러 발생 X)
```

## 널 병합 연산자 (Nullish Coalescing: `??`)

`||` 연산자의 단점을 보완하기 위해 탄생했습니다. 오직 좌항이 **`null`** 또는 **`undefined`**일 때만 우항을 선택합니다.

- **`||` vs `??` 차이점:** `||`는 `0`이나 `""`도 거짓으로 보고 무시하지만, `??`는 이를 유효한 값으로 인정합니다.

```js
const userConfig = {
  opacity: 0,
  title: ""
};

// OR 사용 시: 0을 거짓으로 판단해 100으로 덮어버림
const opacity1 = userConfig.opacity || 100; // 100 (버그 유발 가능)

// Nullish Coalescing 사용 시: 0은 유효한 값으로 유지
const opacity2 = userConfig.opacity ?? 100; // 0
const theme = userConfig.speed ?? "Fast"; // speed가 없으므로 "Fast"
```

## 실무응용
옵셔널 체이닝과 널 병합 연산자를 함께 쓰면 매우 견고한 코드를 짤 수 있습니다.
```js
// API 응답 데이터를 안전하게 처리하는 예시
const apiResponse = {
  data: {
    user: {
      items: []
    }
  }
};

const firstItem = apiResponse?.data?.user?.items?.[0] ?? "No items found";
console.log(firstItem); // "No items found"
```

### 💡 요약 및 추천

1. **조건부 실행:** `&&`를 쓰세요.
    
2. **포괄적 기본값:** `||`를 쓰되, `0`이나 `""`이 값으로 들어올 수 있다면 주의하세요.
    
3. **엄격한 기본값:** `??`를 쓰세요. (가장 권장되는 방식)
    
4. **안전한 객체 접근:** `?.`를 습관화하세요.